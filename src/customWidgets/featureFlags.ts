/** Re-enable in a future phase to show the History tab on Manage Custom Widgets. */
export const ENABLE_CUSTOM_WIDGET_HISTORY_TAB = false;

/** Re-enable in a future phase to show the Label as External Content checkbox on embed widgets. */
export const ENABLE_LABEL_AS_EXTERNAL_CONTENT = false;
